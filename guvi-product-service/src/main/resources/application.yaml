spring:
  application:
    name: guvi-product-service

  datasource:
    url: jdbc:mysql://localhost:3306/products_db
    username: root
    password: guvimysql@2026
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

server:
  port: 8081

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/

